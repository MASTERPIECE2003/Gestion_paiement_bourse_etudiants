spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: michaelramana2021@gmail.com
    password: "bftg krbd frcv faov"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
  datasource:
    url: jdbc:postgresql://localhost:5432/gestionBourse
    username: postgres
    password: "Michaelra"
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

server:
  port: 8082

